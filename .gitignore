# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# AutoForge runtime (never commit live controller state, logs, locks,
# temp files or crash leftovers — exclude the whole directory)
.autoforge/

# The operator's own config (created from autoforge.example.yaml): local to
# one checkout, may name local paths, and is not the example everyone reads
autoforge.yaml

# Local config overrides with possible credentials
autoforge.local.yaml
autoforge.local.yml
autoforge.local.toml
autoforge.local.json
.env
